{
  "name": "santy-erp-frontend",
  "version": "1.0.0",
  "description": "Secure multi-tenant business platform",
  "main": "index.jsx",
  "scripts": {
    "build": "mkdir -p dist && esbuild index.jsx --bundle --outfile=dist/bundle.js --format=esm --loader:.js=jsx --minify && cp index.html dist/index.html"
  },
  "dependencies": {
    "firebase": "^12.5.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}

